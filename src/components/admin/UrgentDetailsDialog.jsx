import {X} from 'lucide-react';
const row=(label,value)=>value!==undefined&&value!==null&&value!==''?<div><dt className="text-xs font-bold uppercase tracking-wide text-muted-foreground">{label}</dt><dd className="mt-1 font-medium">{String(value)}</dd></div>:null;
export default function UrgentDetailsDialog({request,onClose}){if(!request)return null;return <div className="fixed inset-0 z-[1200] grid place-items-center bg-foreground/55 p-4"><section className="max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-3xl bg-card p-6 shadow-2xl"><header className="flex items-start justify-between gap-4"><div><p className="text-sm font-bold text-destructive">{request.reference}</p><h2 className="mt-1 text-2xl font-bold">Demande urgente</h2></div><button onClick={onClose} className="rounded-full border p-2" aria-label="Fermer"><X size={20}/></button></header><dl className="mt-6 grid gap-5 rounded-2xl bg-secondary p-5 sm:grid-cols-2">{row('Client',request.first_name)}{row('E-mail',request.email)}{row('Téléphone',request.phone)}{row('Événement',request.event_type)}{row('Date',request.event_date)}{row('Heure',request.event_time)}{row('Lieu',request.location)}{row('Rayon',request.radius_km?`${request.radius_km} km`:null)}{row('Invités',request.guest_count)}{row('Budget',request.budget?`${request.budget} €`:null)}{row('Besoin',request.service_need)}{row('Niveau d’urgence',request.urgency_level)}{row('Échéance de réponse',request.response_deadline)}{row('Professionnels alertés',request.professionals_alerted)}</dl>{request.reason&&<div className="mt-5 rounded-2xl border p-4"><h3 className="font-bold">Motif</h3><p className="mt-2 whitespace-pre-wrap text-sm">{request.reason}</p></div>}{request.message&&<div className="mt-4 rounded-2xl border p-4"><h3 className="font-bold">Message</h3><p className="mt-2 whitespace-pre-wrap text-sm">{request.message}</p></div>}<button onClick={onClose} className="mt-6 w-full rounded-xl bg-primary px-4 py-3 font-bold text-primary-foreground">Fermer</button></section></div>}
import {useState} from 'react';
import {base44} from '@/api/base44Client';
export default function AdminExtraEmailDialog({item,onClose}){const[subject,setSubject]=useState('Message de Un Bon Traiteur');const[message,setMessage]=useState('');const[busy,setBusy]=useState(false);const[error,setError]=useState('');const send=async e=>{e.preventDefault();setBusy(true);setError('');try{await base44.functions.invoke('extrasHub',{action:'admin_email',profile_id:item.id,subject,message});onClose()}catch(err){setError(err.response?.data?.error||err.message);setBusy(false)}};return <div className="fixed inset-0 z-[1200] grid place-items-center bg-foreground/55 p-4"><form onSubmit={send} className="w-full max-w-lg rounded-3xl bg-card p-6 shadow-2xl"><h2 className="text-2xl font-bold">Écrire à {item.first_name}</h2><p className="mt-1 text-sm text-muted-foreground">Le message sera envoyé à {item.email}.</p><label className="mt-5 block text-sm font-semibold">Objet<input required value={subject} onChange={e=>setSubject(e.target.value)} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"/></label><label className="mt-4 block text-sm font-semibold">Message<textarea required minLength={10} value={message} onChange={e=>setMessage(e.target.value)} className="mt-1 min-h-40 w-full rounded-xl border bg-background p-3"/></label>{error&&<p className="mt-3 text-sm text-destructive">{error}</p>}<div className="mt-5 flex justify-end gap-2"><button type="button" onClick={onClose} className="rounded-xl border px-4 py-2 font-bold">Annuler</button><button disabled={busy} className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground disabled:opacity-50">{busy?'Envoi…':'Envoyer'}</button></div></form></div>}
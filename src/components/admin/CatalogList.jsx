import { Plus, Trash2 } from 'lucide-react';

export default function CatalogList({ title, description, items, value, onValue, onAdd, onToggle, onDelete }) {
  return <section className="rounded-3xl border bg-card/90 p-5 shadow-xl"><h2 className="text-xl font-bold">{title}</h2><p className="mt-1 text-sm text-muted-foreground">{description}</p><form onSubmit={onAdd} className="mt-5 flex gap-2"><input required value={value} onChange={e => onValue(e.target.value)} placeholder="Nouveau libellé" className="h-11 min-w-0 flex-1 rounded-xl border bg-background px-3"/><button className="grid h-11 w-11 place-items-center rounded-xl bg-primary text-primary-foreground" aria-label="Ajouter"><Plus size={18}/></button></form><div className="mt-5 divide-y">{items.map(item => <div key={item.id} className="flex items-center gap-3 py-3"><button type="button" onClick={() => onToggle(item)} className={`h-6 w-11 rounded-full p-1 ${item.active ? 'bg-primary' : 'bg-muted'}`} aria-label={item.active ? 'Désactiver' : 'Activer'}><span className={`block h-4 w-4 rounded-full bg-card transition-transform ${item.active ? 'translate-x-5' : ''}`}/></button><span className={item.active ? 'font-semibold' : 'text-muted-foreground line-through'}>{item.name}</span><button type="button" onClick={() => onDelete(item.id)} className="ml-auto text-destructive" aria-label={`Supprimer ${item.name}`}><Trash2 size={17}/></button></div>)}{!items.length && <p className="py-5 text-sm text-muted-foreground">Aucun élément.</p>}</div></section>;
}
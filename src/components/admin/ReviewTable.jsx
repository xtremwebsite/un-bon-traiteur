const labels = { pending: 'En attente', published: 'Publié', rejected: 'Rejeté' };

export default function ReviewTable({ items, caterers, onUpdate }) {
  const names = Object.fromEntries(caterers.map(item => [item.id, item.business_name]));
  return <section className="overflow-hidden rounded-3xl border bg-card/90 shadow-xl backdrop-blur-xl"><div className="border-b p-5"><h2 className="text-xl font-bold">Modération des avis</h2><p className="mt-1 text-sm text-muted-foreground">Valider, rejeter et certifier les avis clients.</p></div><div className="divide-y">{items.map(review => <article key={review.id} className="p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-bold">{review.reviewer_name} · {review.rating}/5</p><p className="text-sm text-muted-foreground">{names[review.caterer_id] || 'Traiteur inconnu'} · {labels[review.status] || review.status}</p></div><div className="flex gap-2">{review.status !== 'published' && <button onClick={() => onUpdate(review.id, { status: 'published' })} className="rounded-lg bg-primary px-3 py-2 text-sm font-semibold text-primary-foreground">Publier</button>}{review.status !== 'rejected' && <button onClick={() => onUpdate(review.id, { status: 'rejected' })} className="rounded-lg border px-3 py-2 text-sm">Rejeter</button>}</div></div>{review.title && <h3 className="mt-3 font-semibold">{review.title}</h3>}<p className="mt-1 text-sm">{review.comment}</p><label className="mt-3 flex items-center gap-2 text-sm"><input type="checkbox" checked={!!review.verified_booking} onChange={e => onUpdate(review.id, { verified_booking: e.target.checked })}/>Réservation vérifiée</label></article>)}</div>{!items.length && <p className="p-6 text-center text-muted-foreground">Aucun avis.</p>}</section>;
}
import { useEffect,useState } from 'react';
import { Link } from 'react-router-dom';
import { base44 } from '@/api/base44Client';
export default function AdminGuard({children}){const[state,setState]=useState('loading');useEffect(()=>{base44.auth.me().then(user=>setState(user.role==='admin'?'allowed':'denied')).catch(()=>setState('denied'))},[]);if(state==='loading')return <div className="flex min-h-screen items-center justify-center"><div className="h-8 w-8 animate-spin rounded-full border-4 border-border border-t-primary"/></div>;if(state==='denied')return <main className="flex min-h-screen items-center justify-center bg-muted p-6"><div className="max-w-md rounded-3xl border bg-card p-8 text-center shadow-2xl"><h1 className="text-2xl font-bold">Accès refusé</h1><p className="mt-3 text-muted-foreground">Cette page est réservée aux administrateurs.</p><Link to="/" className="mt-6 inline-flex rounded-lg bg-primary px-5 py-3 font-semibold text-primary-foreground">Retour au site</Link></div></main>;return children}
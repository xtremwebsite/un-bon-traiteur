import {useState} from 'react';import {jobOptions,weekOptions} from '@/lib/hr';
const empty={first_name:'',last_name:'',email:'',phone:'',birth_date:'',job_role:'serveur',custom_role:'',experience_years:0,skills:'',weekly_days:[],notes:''};
export default function EmployeeForm({onSubmit}){const[data,setData]=useState(empty);const[busy,setBusy]=useState(false);const[error,setError]=useState('');const set=(key,value)=>setData(x=>({...x,[key]:value}));const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{await onSubmit({...data,skills:data.skills.split(',').map(x=>x.trim()).filter(Boolean)});setData(empty)}catch(err){setError(err.response?.data?.error||err.message)}finally{setBusy(false)}};return <form onSubmit={submit} className="space-y-4"><div className="grid gap-3 sm:grid-cols-2"><label>Prénom<input required value={data.first_name} onChange={e=>set('first_name',e.target.value)} className="mt-1 h-11 w-full rounded-xl border px-3"/></label><label>Nom<input required value={data.last_name} onChange={e=>set('last_name',e.target.value)} className="mt-1 h-11 w-full rounded-xl border px-3"/></label><label>E-mail professionnel<input required type="email" value={data.email} onChange={e=>set('email',e.target.value)} className="mt-1 h-11 w-full rounded-xl border px-3"/></label><label>Téléphone<input value={data.phone} onChange={e=>set('phone',e.target.value)} className="mt-1 h-11 w-full rounded-xl border px-3"/></label><label>Date de naissance<input type="date" value={data.birth_date} onChange={e=>set('birth_date',e.target.value)} className="mt-1 h-11 w-full rounded-xl border px-3"/></label><label>Expérience (années)<input min="0" type="number" value={data.experience_years} onChange={e=>set('experience_years',Number(e.target.value))} className="mt-1 h-11 w-full rounded-xl border px-3"/></label><label>Métier<select value={data.job_role} onChange={e=>set('job_role',e.target.value)} className="mt-1 h-11 w-full rounded-xl border px-3">{jobOptions.map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label>Compétences<input value={data.skills} onChange={e=>set('skills',e.target.value)} placeholder="Cocktail, banquet, cuisine chaude" className="mt-1 h-11 w-full rounded-xl border px-3"/></label></div>{data.job_role==='autre'&&<label className="block">Intitulé du poste<input required value={data.custom_role} onChange={e=>set('custom_role',e.target.value)} className="mt-1 h-11 w-full rounded-xl border px-3"/></label>}<fieldset><legend className="text-sm font-bold">Jours habituellement disponibles</legend><div className="mt-2 flex flex-wrap gap-2">{weekOptions.map(([value,label])=><label key={value} className={`cursor-pointer rounded-xl border px-3 py-2 text-sm ${data.weekly_days.includes(value)?'bg-primary text-primary-foreground':''}`}><input type="checkbox" className="sr-only" checked={data.weekly_days.includes(value)} onChange={()=>set('weekly_days',data.weekly_days.includes(value)?data.weekly_days.filter(x=>x!==value):[...data.weekly_days,value])}/>{label}</label>)}</div></fieldset><label className="block">Notes RH<textarea value={data.notes} onChange={e=>set('notes',e.target.value)} className="mt-1 min-h-20 w-full rounded-xl border p-3"/></label>{error&&<p className="text-sm font-bold text-destructive">{error}</p>}<button disabled={busy} className="w-full rounded-xl bg-primary px-4 py-3 font-bold text-primary-foreground">{busy?'Invitation…':'Créer le compte et envoyer l’invitation'}</button></form>}
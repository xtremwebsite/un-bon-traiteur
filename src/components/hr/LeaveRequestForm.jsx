import {useState} from 'react';const initial={start_date:'',end_date:'',leave_type:'paid_leave',reason:''};
export default function LeaveRequestForm({onSubmit}){const[data,setData]=useState(initial);const[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);await onSubmit(data);setData(initial);setBusy(false)};return <form onSubmit={submit} className="space-y-3"><div className="grid gap-3 sm:grid-cols-2"><label>Du<input required type="date" value={data.start_date} onChange={e=>setData(x=>({...x,start_date:e.target.value}))} className="mt-1 h-11 w-full rounded-xl border px-3"/></label><label>Au<input required type="date" min={data.start_date} value={data.end_date} onChange={e=>setData(x=>({...x,end_date:e.target.value}))} className="mt-1 h-11 w-full rounded-xl border px-3"/></label></div><label className="block">Type<select value={data.leave_type} onChange={e=>setData(x=>({...x,leave_type:e.target.value}))} className="mt-1 h-11 w-full rounded-xl border px-3"><option value="paid_leave">Congés payés</option><option value="unpaid_leave">Sans solde</option><option value="recovery">Récupération</option><option value="sick_leave">Maladie</option><option value="other">Autre</option></select></label><label className="block">Commentaire<textarea value={data.reason} onChange={e=>setData(x=>({...x,reason:e.target.value}))} className="mt-1 min-h-20 w-full rounded-xl border p-3"/></label><button disabled={busy} className="w-full rounded-xl bg-primary px-4 py-3 font-bold text-primary-foreground">{busy?'Envoi…':'Envoyer la demande'}</button></form>}
import {Briefcase,Mail,Phone} from 'lucide-react';import {employeeAge,jobLabel} from '@/lib/hr';
const statusLabels={invited:'Invité',active:'Actif',on_leave:'En congé',inactive:'Inactif'};
export default function EmployeeCard({employee,onStatus}){const age=employeeAge(employee.birth_date);return <article className="rounded-2xl border bg-card p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><div><h3 className="font-bold">{employee.first_name} {employee.last_name}</h3><p className="mt-1 flex items-center gap-2 text-sm text-primary"><Briefcase size={14}/>{employee.job_role==='autre'?employee.custom_role:jobLabel(employee.job_role)}</p></div><select value={employee.status} onChange={e=>onStatus(employee,e.target.value)} className="rounded-lg border bg-background px-2 py-1 text-xs font-bold">{Object.entries(statusLabels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></div><div className="mt-3 space-y-1 text-sm text-muted-foreground"><p>{age?`${age} ans · `:''}{employee.experience_years||0} an(s) d’expérience</p><a href={`mailto:${employee.email}`} className="flex items-center gap-2"><Mail size={14}/>{employee.email}</a>{employee.phone&&<a href={`tel:${employee.phone}`} className="flex items-center gap-2"><Phone size={14}/>{employee.phone}</a>}</div>{employee.skills?.length>0&&<div className="mt-3 flex flex-wrap gap-1">{employee.skills.map(skill=><span key={skill} className="rounded-full bg-secondary px-2 py-1 text-xs">{skill}</span>)}</div>}</article>}
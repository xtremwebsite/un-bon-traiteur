import {Link} from 'react-router-dom';
import {FileText,LayoutDashboard,Siren,UserRound} from 'lucide-react';
import LogoutButton from '@/components/auth/LogoutButton';
const links=[['/mon-espace','Vue d’ensemble',LayoutDashboard],['/mes-devis','Mes devis',FileText],['/mon-espace#urgences','Mes urgences',Siren],['/mon-espace#profil','Mon profil',UserRound]];
export default function AccountSidebar({user}){const initials=(user?.full_name||user?.email||'U').split(/\s|@/).filter(Boolean).slice(0,2).map(x=>x[0].toUpperCase()).join('');return <aside className="space-y-5 lg:sticky lg:top-24"><div className="rounded-[1.75rem] border border-primary-foreground/35 bg-card/15 p-5 text-center text-primary-foreground shadow-2xl backdrop-blur-2xl"><div className="mx-auto grid h-16 w-16 place-items-center rounded-full bg-secondary text-xl font-black text-primary shadow-lg">{initials}</div><p className="mt-3 font-bold">{user?.full_name||'Mon compte'}</p><p className="mt-1 truncate text-xs opacity-75">{user?.email}</p><Link to="/" className="mt-4 block rounded-xl bg-secondary px-4 py-3 text-sm font-bold text-primary">Voir le site</Link><LogoutButton className="mt-2 w-full text-primary-foreground hover:bg-primary-foreground/10"/></div><nav className="rounded-[1.75rem] bg-primary/75 p-3 text-primary-foreground shadow-xl backdrop-blur-xl">{links.map(([to,label,Icon],index)=><Link key={to} to={to} className={`flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-bold ${index===0?'bg-secondary text-primary':'hover:bg-primary-foreground/10'}`}><Icon size={17}/>{label}</Link>)}</nav></aside>;}
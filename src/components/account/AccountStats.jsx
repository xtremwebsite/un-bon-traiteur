import {CalendarCheck,CheckCircle2,FileClock,MessageSquareText} from 'lucide-react';
const cards=[['Demandes actives',FileClock,'active'],['Réponses reçues',MessageSquareText,'responses'],['Événements à venir',CalendarCheck,'upcoming'],['Demandes terminées',CheckCircle2,'completed']];
export default function AccountStats({requests}){const now=new Date();const values={active:requests.filter(x=>!['closed','cancelled','fulfilled','expired'].includes(x.status)).length,responses:requests.filter(x=>x.status==='responses_received').length,upcoming:requests.filter(x=>x.event_date&&new Date(x.event_date)>=now).length,completed:requests.filter(x=>['closed','fulfilled'].includes(x.status)).length};return <section className="grid grid-cols-2 gap-3 md:grid-cols-4">{cards.map(([label,Icon,key],index)=><article key={key} className="min-h-36 rounded-[1.75rem] border border-primary-foreground/35 bg-card/15 p-5 text-primary-foreground shadow-xl backdrop-blur-2xl"><span className={`grid h-10 w-10 place-items-center rounded-xl ${index%2?'bg-chart-4 text-primary':'bg-destructive text-destructive-foreground'}`}><Icon size={20}/></span><p className="mt-5 text-sm opacity-80">{label}</p><strong className="mt-1 block text-3xl">{values[key]}</strong></article>)}</section>;}
const Row=({label,value})=>value?<div><dt className="text-xs font-bold uppercase tracking-wide text-muted-foreground">{label}</dt><dd className="mt-1 text-sm font-semibold">{value}</dd></div>:null;
export default function QuoteDetailSummary({item}){return <div className="grid gap-4 rounded-2xl bg-secondary/50 p-4 sm:grid-cols-2 lg:grid-cols-3"><Row label="Client" value={`${item.first_name||''} ${item.last_name||''}`.trim()}/><Row label="E-mail" value={item.email}/><Row label="Téléphone" value={item.phone}/><Row label="Événement" value={item.event_type}/><Row label="Date et heure" value={`${item.event_date||''} ${item.event_time||''}`.trim()}/><Row label="Invités" value={item.guest_count}/><Row label="Lieu" value={[item.address,item.postal_code,item.city].filter(Boolean).join(', ')||item.location}/><Row label="Budget" value={item.budget?`${Number(item.budget).toLocaleString('fr-FR')} € ${item.budget_type==='per_person'?'par personne':'au total'}`:null}/><Row label="Format" value={item.format}/><Row label="Cuisine" value={item.cuisine}/><Row label="Services" value={item.services?.join(', ')}/><Row label="Contraintes" value={item.dietary_needs?.join(', ')}/>{item.message&&<div className="sm:col-span-2 lg:col-span-3"><Row label="Demande complète" value={item.message}/></div>}</div>}
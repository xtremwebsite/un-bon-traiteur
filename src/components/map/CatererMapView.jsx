import 'leaflet/dist/leaflet.css';import {CircleMarker,MapContainer,Popup,TileLayer} from 'react-leaflet';
export default function CatererMapView({caterers}){return <MapContainer center={[46.6,2.2]} zoom={6} scrollWheelZoom className="h-[65vh] min-h-[480px] w-full rounded-3xl"><TileLayer attribution='&copy; OpenStreetMap' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/>{caterers.map(item=>{const destination=item.address?`${item.address}, ${item.postal_code||''} ${item.city}`:`${item.latitude},${item.longitude}`;return <CircleMarker key={item.id} center={[item.latitude,item.longitude]} radius={9} pathOptions={{color:'#173d31',fillColor:'#e5685c',fillOpacity:1}}><Popup><strong>{item.business_name}</strong><br/>{item.address&&<><span>{item.address}</span><br/></>}<span>{item.postal_code} {item.city}</span><div className="mt-3 flex gap-2"><a href={`/traiteurs/${item.slug}`} className="font-semibold text-primary">Voir la fiche</a><a href={`https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(destination)}`} target="_blank" rel="noreferrer" className="font-semibold text-primary">M’y rendre</a></div></Popup></CircleMarker>})}</MapContainer>}
import {Calendar,Euro,MapPin,Users} from 'lucide-react';
import {base44} from '@/api/base44Client';

const statuses=[['submitted','Nouveau'],['matched','À traiter'],['responses_received','En discussion'],['closed','Accepté'],['cancelled','Refusé']];
export default function QuoteKanbanCard({item,onOpen,onChanged}){
  const move=async event=>{event.stopPropagation();const r=await base44.functions.invoke('quoteConversation',{action:'status',quote_id:item.id,status:event.target.value});onChanged(r.data.item)};
  const urgentRequest=String(item.reference||'').startsWith('DISC-URGENT-');
  return <article className={`rounded-2xl border p-3 shadow-sm ${urgentRequest?'border-destructive/40 bg-destructive/10':'border-primary/30 bg-primary/10'}`}><button onClick={()=>onOpen(item)} className="w-full text-left"><p className="text-xs font-bold text-destructive">{item.reference}</p><h4 className="mt-1 font-bold text-primary">{item.first_name} {item.last_name}</h4><p className="truncate text-xs text-muted-foreground">{item.email}</p><div className="mt-3 space-y-1.5 text-xs"><p className="flex gap-2"><Calendar size={14}/>{item.event_date} · {item.event_type}</p><p className="flex gap-2"><MapPin size={14}/>{item.location}</p><p className="flex gap-2"><Users size={14}/>{item.guest_count} personnes</p>{item.budget&&<p className="flex gap-2"><Euro size={14}/>{Number(item.budget).toLocaleString('fr-FR')} €</p>}</div><p className="mt-3 text-xs font-bold">Ouvrir le dossier →</p></button><select aria-label="Changer le statut" value={item.status==='draft'?'submitted':item.status} onChange={move} className="mt-3 h-9 w-full rounded-lg border bg-background px-2 text-xs font-bold">{statuses.map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></article>;
}
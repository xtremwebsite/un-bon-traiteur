import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import {base44} from '@/api/base44Client';import ProBookingFeed from '@/components/extras/ProBookingFeed';import ProRequestFeed from '@/components/extras/ProRequestFeed';
export default function DashboardExtraTracking(){const[data,setData]=useState({requests:[],bookings:[]});const[loading,setLoading]=useState(true);const load=()=>base44.functions.invoke('extrasHub',{action:'directory'}).then(r=>setData({requests:r.data.requests||[],bookings:r.data.bookings||[]})).finally(()=>setLoading(false));useEffect(()=>{load();return base44.entities.ExtraBooking.subscribe(()=>load())},[]);const action=async(type,item,status)=>{if(type!=='respond_booking'&&!window.confirm(type==='cancel_booking'?'Confirmer l’annulation ?':'Supprimer cette demande ?'))return;await base44.functions.invoke('extrasHub',{action:type,booking_id:item.id,status});await load()};return <section id="suivi-extras" className="scroll-mt-24"><div className="mb-3 flex items-center justify-between"><div><p className="text-sm font-bold uppercase tracking-widest text-destructive">Personnel Extra</p><h2 className="text-2xl font-bold">Candidatures et réservations</h2></div><Link to="/suivi-extras" className="text-sm font-bold text-primary">Tout l’historique →</Link></div>{loading?<div className="h-32 animate-pulse rounded-3xl bg-muted"/>:<><ProRequestFeed items={data.requests} bookings={data.bookings}/><ProBookingFeed items={data.bookings} onRespond={(item,status)=>action('respond_booking',item,status)} onCancel={item=>action('cancel_booking',item)} onDelete={item=>action('delete_booking',item)}/></>}</section>}
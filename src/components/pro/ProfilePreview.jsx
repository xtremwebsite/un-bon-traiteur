import { Image } from '@/components/ui/image';
import { MapPin, Utensils } from 'lucide-react';

const fallback = 'https://images.unsplash.com/photo-1555244162-803834f70033?auto=format&fit=crop&w=1200&q=85';

export default function ProfilePreview({ profile }) {
  return <aside className="overflow-hidden rounded-[2rem] border bg-background shadow-2xl lg:sticky lg:top-24"><div className="relative h-56"><Image src={profile.hero_image || fallback} alt={`Couverture de ${profile.business_name}`} className="h-full w-full" fittingType="fill"/><div className="absolute inset-0 bg-gradient-to-t from-foreground/60 to-transparent"/><div className="absolute -bottom-9 left-6 grid h-20 w-20 place-items-center overflow-hidden rounded-3xl border-4 border-background bg-card text-primary shadow-xl">{profile.logo_url ? <Image src={profile.logo_url} alt={`Logo ${profile.business_name}`} className="h-full w-full" fittingType="fit"/> : <Utensils size={32}/>}</div></div><div className="px-6 pb-7 pt-14"><p className="text-xs font-bold uppercase tracking-widest text-destructive">Aperçu de votre fiche</p><h2 className="mt-2 font-heading text-3xl font-bold">{profile.business_name}</h2><p className="mt-2 flex items-center gap-2 text-sm text-muted-foreground"><MapPin size={16}/>{profile.city || 'Ville à renseigner'}</p><p className="mt-5 line-clamp-3 text-sm leading-6 text-muted-foreground">{profile.description || 'Votre présentation apparaîtra ici.'}</p><div className="mt-6 grid grid-cols-2 gap-3">{(profile.gallery || []).slice(0,4).map((src, index) => <Image key={src} src={src} alt={`Réalisation ${index + 1}`} className={`${index === 0 ? 'col-span-2 h-40' : 'h-28'} w-full rounded-2xl`} fittingType="fill"/>)}{!profile.gallery?.length && <div className="col-span-2 grid h-40 place-items-center rounded-2xl bg-secondary text-sm font-semibold text-primary">Vos réalisations</div>}</div></div></aside>;
}
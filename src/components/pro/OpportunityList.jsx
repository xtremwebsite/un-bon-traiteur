import { CalendarDays, LockKeyhole, MapPin, Users } from 'lucide-react';

export default function OpportunityList({ items, subscribed }) {
  return <aside className="max-h-[680px] space-y-3 overflow-y-auto pr-1">{items.map(item=><article key={`${item.kind}-${item.id}`} className="rounded-3xl border bg-card p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className={`text-xs font-bold uppercase tracking-wider ${item.kind==='urgent'?'text-destructive':'text-primary'}`}>{item.kind==='urgent'?'Urgence':'Devis simple'}</p><h2 className="mt-1 font-bold">{item.event_type}</h2></div><span className="rounded-full bg-secondary px-3 py-1 text-xs font-bold text-primary">{item.reference}</span></div><div className="mt-4 grid gap-2 text-sm text-muted-foreground"><span className="flex gap-2"><CalendarDays size={16}/>{item.event_date}</span><span className="flex gap-2"><MapPin size={16}/>{item.location}{Number.isFinite(item.distance)&&` · ${Math.round(item.distance)} km`}</span><span className="flex gap-2"><Users size={16}/>{item.guest_count} invités</span></div>{subscribed?<div className="mt-4 border-t pt-4 text-sm"><p>{item.message||'Aucune précision supplémentaire.'}</p><p className="mt-2 font-semibold">{item.first_name} {item.last_name}</p><a href={`mailto:${item.email}`} className="text-primary underline">{item.email}</a>{item.phone&&<a href={`tel:${item.phone}`} className="ml-3 text-primary underline">{item.phone}</a>}</div>:<p className="mt-4 flex gap-2 rounded-xl bg-muted p-3 text-sm"><LockKeyhole size={16}/>Contact disponible avec un abonnement actif.</p>}</article>)}{!items.length&&<div className="rounded-3xl border bg-card p-8 text-center text-muted-foreground">Aucune demande ne correspond à ces filtres.</div>}</aside>;
}
import { useState } from 'react';
export default function CookieBanner(){const[open,setOpen]=useState(()=>!localStorage.getItem('cookie-consent'));const choose=(value)=>{localStorage.setItem('cookie-consent',JSON.stringify({value,date:new Date().toISOString(),version:'1.0'}));setOpen(false)};if(!open)return null;return <section role="dialog" aria-label="Gestion des cookies" className="fixed inset-x-3 bottom-3 z-50 mx-auto max-w-3xl rounded-2xl border bg-card p-5 shadow-2xl"><h2 className="font-bold">Vos choix de confidentialité</h2><p className="mt-2 text-sm text-muted-foreground">Les cookies essentiels assurent le fonctionnement du site. Aucun traceur non essentiel n’est chargé sans votre accord.</p><div className="mt-4 flex flex-wrap gap-3"><button onClick={()=>choose('accepted')} className="rounded-lg bg-primary px-4 py-2 font-semibold text-primary-foreground">Tout accepter</button><button onClick={()=>choose('refused')} className="rounded-lg border border-primary px-4 py-2 font-semibold text-primary">Tout refuser</button><button onClick={()=>choose('essential')} className="rounded-lg px-4 py-2 font-semibold underline">Personnaliser</button></div></section>}
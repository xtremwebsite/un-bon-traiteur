import { Euro, Sparkles, Users } from 'lucide-react';

const price = value => new Intl.NumberFormat('fr-FR', { minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(Number(value));

export default function CatererPricingCard({ caterer }) {
  const items = (caterer.format_pricing || []).filter(item => (caterer.formats || []).includes(item.name) && (item.min_guests || item.max_guests || item.price_from != null));
  if (!items.length) return null;
  return <section className="relative overflow-hidden rounded-[2rem] border bg-card/75 p-6 shadow-2xl backdrop-blur-2xl"><div className="absolute -right-10 -top-10 h-28 w-28 rounded-full bg-chart-4/30 blur-2xl"/><div className="absolute -bottom-12 -left-8 h-24 w-24 rounded-full bg-primary/20 blur-2xl"/><div className="relative"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-primary text-primary-foreground shadow-lg"><Sparkles size={19}/></span><div><p className="text-xs font-bold uppercase tracking-widest text-destructive">Prix / personne</p><h2 className="font-heading text-xl font-bold">Restauration & conditions</h2></div></div><div className="mt-5 space-y-3">{items.map(item => <div key={item.name} className="rounded-2xl border bg-background/65 p-4 shadow-lg backdrop-blur-xl"><div className="flex items-start justify-between gap-3"><strong className="text-sm text-primary">{item.name}</strong>{item.price_from != null && item.price_from !== '' && <span className="shrink-0 rounded-full bg-primary px-3 py-1 text-xs font-bold text-primary-foreground">Dès {price(item.price_from)} €</span>}</div>{(item.min_guests || item.max_guests) && <p className="mt-2 flex items-center gap-2 text-xs text-muted-foreground"><Users size={14}/>{item.min_guests || '—'} à {item.max_guests || '—'} personnes</p>}</div>)}</div><p className="mt-4 flex items-center gap-2 text-xs text-muted-foreground"><Euro size={14}/>Tarifs indicatifs, devis personnalisé selon votre événement.</p></div></section>;
}
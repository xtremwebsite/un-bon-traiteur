import { Sparkles } from 'lucide-react';

const price = value => new Intl.NumberFormat('fr-FR', { minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(Number(value));

export default function CatererPricingCard({ caterer }) {
  const items = (caterer.format_pricing || []).filter(item => (caterer.formats || []).includes(item.name) && (item.min_guests || item.max_guests || item.price_from != null));
  if (!items.length) return null;
  return <section className="relative overflow-hidden rounded-3xl border bg-card/80 p-4 shadow-xl backdrop-blur-2xl"><div className="absolute -right-8 -top-8 h-20 w-20 rounded-full bg-chart-4/25 blur-2xl"/><div className="relative"><div className="flex items-center gap-2"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-xl bg-primary text-primary-foreground shadow-md"><Sparkles size={15}/></span><div><p className="text-[10px] font-bold uppercase tracking-widest text-destructive">Prix / personne</p><h2 className="font-heading text-base font-bold">Restauration & conditions</h2></div></div><div className="mt-3 divide-y rounded-2xl border bg-background/65 px-3 backdrop-blur-xl">{items.map(item => <div key={item.name} className="flex flex-wrap items-center gap-x-2 gap-y-1 py-2 text-sm"><strong className="text-primary">{item.name}</strong>{(item.min_guests || item.max_guests) && <span className="text-muted-foreground">· de {item.min_guests || '—'} à {item.max_guests || '—'} pers.</span>}{item.price_from != null && item.price_from !== '' && <span className="ml-auto shrink-0 font-bold text-primary">dès {price(item.price_from)} €</span>}</div>)}</div><p className="mt-2 text-[11px] text-muted-foreground">Tarifs indicatifs · devis personnalisé selon l’événement.</p></div></section>;
}
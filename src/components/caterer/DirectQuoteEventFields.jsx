import BudgetPerPersonHint from '@/components/forms/BudgetPerPersonHint';

export default function DirectQuoteEventFields({ data, set, events, formats }) {
  const today = new Date().toISOString().slice(0, 10);
  return <fieldset className="rounded-2xl border bg-secondary/40 p-4"><legend className="px-2 text-sm font-bold text-primary">Votre événement</legend><div className="grid gap-3 sm:grid-cols-2"><label className="text-sm font-semibold">Type d’événement<select required value={data.event_type} onChange={set('event_type')} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"><option value="">Sélectionner</option>{events.map(item=><option key={item} value={item}>{item}</option>)}</select></label><label className="text-sm font-semibold">Format<select required value={data.format} onChange={set('format')} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"><option value="">Sélectionner</option>{formats.map(item=><option key={item} value={item}>{item}</option>)}</select></label><label className="text-sm font-semibold">Date<input required min={today} type="date" value={data.event_date} onChange={set('event_date')} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"/></label><label className="text-sm font-semibold">Heure<input type="time" value={data.event_time} onChange={set('event_time')} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"/></label><label className="text-sm font-semibold">Nombre d’invités<input required min="1" type="number" value={data.guest_count} onChange={set('guest_count')} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"/></label><label className="text-sm font-semibold">Budget total indicatif<input min="0" type="number" value={data.budget} onChange={set('budget')} className="mt-1 h-11 w-full rounded-xl border bg-background px-3" placeholder="En euros"/></label></div><div className="mt-3"><BudgetPerPersonHint budget={data.budget} guests={data.guest_count}/></div></fieldset>;
}
import {useState} from 'react';import {CheckCircle2} from 'lucide-react';import {base44} from '@/api/base44Client';
export default function QuoteLeadForm({caterer}){const params=new URLSearchParams(window.location.search);const[data,setData]=useState({event_type:params.get('event')||caterer.event_types?.[0]||'Mariage',event_date:params.get('date')||'',location:params.get('location')||'',guest_count:params.get('guests')||'',first_name:'',email:'',caterer_id:caterer.id});const[busy,setBusy]=useState(false);const[result,setResult]=useState('');const[error,setError]=useState('');const set=(key)=>(e)=>setData(current=>({...current,[key]:e.target.value}));const submit=async(e)=>{e.preventDefault();setBusy(true);setError('');try{const response=await base44.functions.invoke('createPublicRequest',{kind:'quote',data:{...data,guest_count:Number(data.guest_count),message:`Demande adressée à ${caterer.business_name}`},website:''});setResult(response.data.reference)}catch(err){setError(err.message||'Envoi impossible')}finally{setBusy(false)}};if(result)return <div className="py-8 text-center"><CheckCircle2 className="mx-auto text-primary" size={42}/><h3 className="mt-3 text-xl font-bold">Demande envoyée</h3><p className="mt-2 text-sm text-muted-foreground">Référence {result}</p></div>;return <form onSubmit={submit} className="space-y-3"><div><p className="text-xs font-bold uppercase tracking-widest text-destructive">Devis gratuit</p><h2 className="mt-1 font-heading text-2xl font-bold">Votre projet avec {caterer.business_name}</h2><p className="mt-2 text-sm text-muted-foreground">Le traiteur est déjà sélectionné.</p></div><label className="block text-sm font-semibold">Événement<select required value={data.event_type} onChange={set('event_type')} className="mt-1 h-11 w-full rounded-xl border bg-background px-3">{(caterer.event_types?.length?caterer.event_types:['Mariage','Entreprise','Réception privée']).map(x=><option key={x}>{x}</option>)}</select></label><div className="grid grid-cols-2 gap-3"><label className="text-sm font-semibold">Date<input required type="date" value={data.event_date} onChange={set('event_date')} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"/></label><label className="text-sm font-semibold">Invités<input required min="1" type="number" value={data.guest_count} onChange={set('guest_count')} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"/></label></div><label className="block text-sm font-semibold">Lieu<input required value={data.location} onChange={set('location')} className="mt-1 h-11 w-full rounded-xl border bg-background px-3" placeholder="Ville de l’événement"/></label><div className="grid grid-cols-2 gap-3"><label className="text-sm font-semibold">Prénom<input required value={data.first_name} onChange={set('first_name')} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"/></label><label className="text-sm font-semibold">Email<input required type="email" value={data.email} onChange={set('email')} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"/></label></div>{error&&<p className="text-sm text-destructive">{error}</p>}<button disabled={busy} className="w-full rounded-xl bg-destructive px-5 py-3 font-bold text-destructive-foreground shadow-lg disabled:opacity-50">{busy?'Envoi…':'Recevoir mon devis'}</button><p className="text-center text-xs text-muted-foreground">Sans engagement · Données protégées</p></form>}
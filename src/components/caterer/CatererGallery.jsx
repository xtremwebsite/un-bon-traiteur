import { Image } from '@/components/ui/image';
export default function CatererGallery({images=[],businessName}){if(!images.length)return null;return <section className="mx-auto max-w-6xl px-4 pt-8"><div className="mb-4 flex items-end justify-between gap-4"><div><p className="text-sm font-semibold text-primary">L’art de recevoir</p><h2 className="mt-1 font-heading text-2xl font-bold">Le savoir-faire en images</h2></div><p className="hidden text-sm text-muted-foreground sm:block">Quelques réalisations</p></div><div className="grid grid-cols-2 gap-3 md:grid-cols-4">{images.slice(0,4).map((src,index)=><div key={src} className="group aspect-square overflow-hidden rounded-2xl border bg-card shadow-sm"><Image src={src} alt={`Réalisation ${index+1} de ${businessName}`} className="h-full w-full transition-transform duration-500 group-hover:scale-105" fittingType="fill"/></div>)}</div></section>}
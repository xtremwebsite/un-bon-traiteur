import { Link } from 'react-router-dom';
import { BadgeCheck,MapPin } from 'lucide-react';
import { Image } from '@/components/ui/image';
const fallback='https://images.unsplash.com/photo-1555244162-803834f70033?auto=format&fit=crop&w=1800&q=85';
export default function CatererHero({item}){return <section className="relative min-h-[520px] overflow-hidden bg-primary"><Image src={item.hero_image||fallback} alt={`Réception réalisée par ${item.business_name}`} className="absolute inset-0 h-full w-full" fittingType="fill"/><div className="absolute inset-0 bg-foreground/60"/><div className="relative mx-auto flex min-h-[520px] max-w-6xl flex-col justify-end px-4 py-12 text-primary-foreground"><p className="text-sm opacity-80">Accueil / Traiteurs / {item.city}</p><div className="mt-5 flex flex-wrap items-end justify-between gap-6"><div className="max-w-3xl"><h1 className="font-heading text-4xl font-bold leading-tight md:text-5xl">{item.business_name}</h1><p className="mt-4 flex items-center gap-2 text-lg"><MapPin size={20}/>{item.city} · rayon de {item.service_radius_km} km</p>{item.verified&&<p className="mt-3 flex items-center gap-2 font-semibold"><BadgeCheck/>Professionnel vérifié</p>}{item.demo&&<span className="mt-4 inline-block rounded-full bg-background/90 px-3 py-1 text-xs text-foreground">Démonstration · non indexable</span>}</div><Link to={`/demande-devis?caterer=${item.id}`} className="rounded-xl bg-destructive px-6 py-3 font-bold text-destructive-foreground shadow-lg">Demander un devis</Link></div></div></section>}
export default function CatererFAQ({item}){const place=item.service_radius_km?`${item.city} et dans un rayon d’environ ${item.service_radius_km} km`:`${item.city} et ses alentours`;const capacity=item.min_guests&&item.max_guests?`Les prestations accueillent généralement de ${item.min_guests} à ${item.max_guests} personnes.`:'La capacité dépend du format et du lieu de votre événement. Précisez votre nombre d’invités dans la demande de devis.';const price=item.price_from_per_person?`Les prestations débutent à ${item.price_from_per_person} € par personne, selon la formule choisie.`:'Les tarifs sont établis sur devis selon la date, le nombre d’invités et les services souhaités.';const defaults=[['Où ce traiteur intervient-il ?',`La zone d’intervention annoncée couvre ${place}.`],['Quels formats sont proposés ?',item.formats?.length?`Les formats indiqués sont : ${item.formats.join(', ')}.`:'Les formats sont personnalisés selon votre réception.'],['Quelle est la capacité d’accueil ?',capacity],['Comment obtenir un tarif précis ?',price]];const faqs=item.faq?.length?item.faq.map(x=>[x.question,x.answer]):defaults;return <section className="mt-12"><p className="text-sm font-bold uppercase tracking-widest text-destructive">Questions fréquentes</p><h2 className="mt-2 font-heading text-3xl font-bold">Tout savoir avant de réserver</h2><div className="mt-6 divide-y rounded-3xl border bg-card px-6 shadow-sm">{faqs.map(([question,answer])=><details key={question} className="group py-5"><summary className="flex cursor-pointer list-none items-center justify-between gap-4 font-bold">{question}<span className="text-2xl font-light text-primary transition-transform group-open:rotate-45">+</span></summary><p className="mt-3 max-w-2xl pr-8 text-sm leading-6 text-muted-foreground">{answer}</p></details>)}</div></section>}
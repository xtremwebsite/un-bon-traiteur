import {CalendarCheck,Check,X} from 'lucide-react';
import {formatAvailabilityDate} from '@/lib/extraAvailability';
const labels={pending:'En attente',confirmed:'Confirmée',declined:'Refusée'};
export default function ExtraBookingInbox({items,onRespond}){
  return <section id="reservations" className="scroll-mt-24 rounded-3xl border bg-secondary/30 p-5"><div className="flex items-center gap-2"><CalendarCheck className="text-primary"/><h2 className="text-xl font-bold">Demandes de réservation</h2></div>{items.length?<div className="mt-4 grid gap-3">{items.map(item=><article key={item.id} className="rounded-2xl border bg-card p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="font-bold">{item.caterer_name}</p><p className="text-sm capitalize text-muted-foreground">{formatAvailabilityDate(item.booking_date)} · {item.period==='evening'?'Soirée':item.period==='both'?'Journée et soirée':'Journée'}</p></div><span className={`rounded-full px-3 py-1 text-xs font-bold ${item.status==='confirmed'?'bg-destructive text-destructive-foreground':item.status==='pending'?'bg-chart-5 text-primary':'bg-muted text-muted-foreground'}`}>{labels[item.status]}</span></div>{item.status==='pending'&&<div className="mt-3 flex gap-2"><button type="button" onClick={()=>onRespond(item,'confirmed')} className="flex items-center gap-1 rounded-xl bg-primary px-3 py-2 text-sm font-bold text-primary-foreground"><Check size={16}/>Accepter</button><button type="button" onClick={()=>onRespond(item,'declined')} className="flex items-center gap-1 rounded-xl border px-3 py-2 text-sm font-bold"><X size={16}/>Refuser</button></div>}</article>)}</div>:<p className="mt-3 text-sm text-muted-foreground">Aucune demande de réservation reçue.</p>}</section>;
}
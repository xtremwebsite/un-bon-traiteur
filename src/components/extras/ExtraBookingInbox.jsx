import {CalendarCheck} from 'lucide-react';
import ExtraBookingCard from '@/components/extras/ExtraBookingCard';
export default function ExtraBookingInbox({items,onRespond,onCancel,onDelete}){const active=items.filter(x=>['pending','confirmed'].includes(x.status));const history=items.filter(x=>!['pending','confirmed'].includes(x.status));const list=data=><div className="mt-4 grid gap-3">{data.map(item=><ExtraBookingCard key={item.id} item={item} onRespond={onRespond} onCancel={onCancel} onDelete={onDelete}/>)}</div>;return <section id="reservations" className="scroll-mt-24 rounded-3xl border bg-secondary/30 p-5"><div className="flex items-center gap-2"><CalendarCheck className="text-primary"/><h2 className="text-xl font-bold">Demandes de réservation</h2></div>{active.length?list(active):<p className="mt-3 text-sm text-muted-foreground">Aucune demande en cours.</p>}{history.length>0&&<><h3 className="mt-6 border-t pt-5 font-bold">Historique</h3>{list(history)}</>}</section>;}
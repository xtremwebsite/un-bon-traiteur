import {X} from 'lucide-react';
import {Image} from '@/components/ui/image';

export default function ExtraProfilePreviewDialog({extra,onClose}){const name=[extra.first_name||'Extra',extra.last_name].filter(Boolean).join(' ');return <div className="fixed inset-0 z-[1200] grid place-items-center bg-foreground/60 p-4" role="dialog" aria-modal="true" aria-label={`Profil de ${name}`}><div className="relative max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-3xl bg-card p-5 shadow-2xl"><button type="button" onClick={onClose} aria-label="Fermer" className="absolute right-4 top-4 z-10 rounded-full bg-card p-2 shadow"><X/></button>{extra.photo_urls?.[0]?<Image src={extra.photo_urls[0]} alt={name} className="h-[420px] w-full rounded-2xl"/>:<div className="grid h-72 place-items-center rounded-2xl bg-secondary text-7xl font-bold text-primary">{extra.first_name?.[0]||'E'}</div>}<h2 className="mt-5 text-2xl font-bold">{name}</h2><p className="mt-1 text-muted-foreground">{extra.city}{extra.experience_years?` · ${extra.experience_years} ans d’expérience`:''}</p><div className="mt-4 flex flex-wrap gap-2">{(extra.skills||[]).map(skill=><span key={skill} className="rounded-full border px-3 py-1 text-sm">{skill}</span>)}</div>{extra.experience_details&&<p className="mt-4">{extra.experience_details}</p>}</div></div>;}
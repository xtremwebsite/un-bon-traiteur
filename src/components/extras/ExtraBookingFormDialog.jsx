import {useState} from 'react';
import {Briefcase,Clock,MapPin,MessageSquare,Timer,X} from 'lucide-react';
import {formatAvailabilityDate} from '@/lib/extraAvailability';
export default function ExtraBookingFormDialog({date,extra,busy,error,onSubmit,onClose}){
  const[job,setJob]=useState(extra.skills?.[0]||'');const[location,setLocation]=useState('');const[time,setTime]=useState('');const[duration,setDuration]=useState('');const[message,setMessage]=useState('');
  if(!date)return null;
  const submit=()=>onSubmit({date,job,location:location.trim(),time,duration,message:message.trim()});
  return <div className="fixed inset-0 z-[1200] grid place-items-center bg-foreground/50 p-4" role="dialog" aria-modal="true" onClick={onClose}><div className="w-full max-w-md rounded-2xl border bg-card p-4 shadow-2xl" onClick={event=>event.stopPropagation()}><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-widest text-destructive">Prestation prévue</p><h3 className="mt-1 text-xl font-bold capitalize">{formatAvailabilityDate(date)}</h3></div><button type="button" onClick={onClose} aria-label="Fermer" className="rounded-full p-2 hover:bg-secondary"><X size={20}/></button></div><div className="mt-5 space-y-4"><label className="block text-sm font-bold"><span className="flex items-center gap-2"><Briefcase size={16}/>Métier validé de l’Extra</span><select value={job} onChange={event=>setJob(event.target.value)} className="mt-2 h-11 w-full rounded-xl border bg-background px-3 font-normal"><option value="">Sélectionner un métier</option>{(extra.skills||[]).map(skill=><option key={skill} value={skill}>{skill}</option>)}</select></label><div className="grid gap-3 sm:grid-cols-[1fr_110px_90px]"><label className="block text-sm font-bold"><span className="flex items-center gap-2"><MapPin size={16}/>Lieu</span><input value={location} onChange={event=>setLocation(event.target.value)} placeholder="Adresse ou ville" className="mt-2 h-10 w-full rounded-xl border bg-background px-3 font-normal"/></label><label className="block text-sm font-bold"><span className="flex items-center gap-2"><Clock size={16}/>Heure</span><input type="time" value={time} onChange={event=>setTime(event.target.value)} className="mt-2 h-10 w-full rounded-xl border bg-background px-2 font-normal"/></label><label className="block text-sm font-bold"><span className="flex items-center gap-2"><Timer size={16}/>Durée</span><input type="number" min="1" max="24" step="0.5" value={duration} onChange={event=>setDuration(event.target.value)} placeholder="h" className="mt-2 h-10 w-full rounded-xl border bg-background px-2 font-normal"/></label></div><label className="block text-sm font-bold"><span className="flex items-center gap-2"><MessageSquare size={16}/>Message pour l’Extra</span><textarea value={message} onChange={event=>setMessage(event.target.value)} placeholder="Horaires, tenue, consignes ou informations utiles…" className="mt-2 min-h-28 w-full rounded-xl border bg-background p-3 font-normal"/></label></div>{error&&<p className="mt-4 rounded-xl bg-destructive/10 p-3 text-sm font-semibold text-destructive">{error}</p>}<div className="mt-5 flex flex-wrap gap-2"><button type="button" onClick={submit} disabled={!job||!location.trim()||!time||!duration||busy} className="rounded-xl bg-primary px-4 py-2.5 font-bold text-primary-foreground disabled:opacity-40">{busy?'Envoi…':'Envoyer la demande'}</button><button type="button" onClick={onClose} className="rounded-xl border px-4 py-2.5 font-bold">Annuler</button></div></div></div>;
}
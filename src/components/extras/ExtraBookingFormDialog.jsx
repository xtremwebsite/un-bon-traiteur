import {useState} from 'react';
import {Briefcase,MapPin,MessageSquare,X} from 'lucide-react';
import {formatAvailabilityDate} from '@/lib/extraAvailability';
export default function ExtraBookingFormDialog({date,extra,busy,error,onSubmit,onClose}){
  const[job,setJob]=useState(extra.skills?.[0]||'');const[location,setLocation]=useState('');const[message,setMessage]=useState('');
  if(!date)return null;
  const submit=()=>onSubmit({date,job,location:location.trim(),message:message.trim()});
  return <div className="fixed inset-0 z-[1200] grid place-items-center bg-foreground/50 p-4" role="dialog" aria-modal="true" onClick={onClose}><div className="w-full max-w-lg rounded-3xl border bg-card p-5 shadow-2xl" onClick={event=>event.stopPropagation()}><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-widest text-destructive">Prestation prévue</p><h3 className="mt-1 text-xl font-bold capitalize">{formatAvailabilityDate(date)}</h3></div><button type="button" onClick={onClose} aria-label="Fermer" className="rounded-full p-2 hover:bg-secondary"><X size={20}/></button></div><div className="mt-5 space-y-4"><label className="block text-sm font-bold"><span className="flex items-center gap-2"><Briefcase size={16}/>Métier validé de l’Extra</span><select value={job} onChange={event=>setJob(event.target.value)} className="mt-2 h-11 w-full rounded-xl border bg-background px-3 font-normal"><option value="">Sélectionner un métier</option>{(extra.skills||[]).map(skill=><option key={skill} value={skill}>{skill}</option>)}</select></label><label className="block text-sm font-bold"><span className="flex items-center gap-2"><MapPin size={16}/>Lieu de la prestation</span><input value={location} onChange={event=>setLocation(event.target.value)} placeholder="Adresse ou ville" className="mt-2 h-11 w-full rounded-xl border bg-background px-3 font-normal"/></label><label className="block text-sm font-bold"><span className="flex items-center gap-2"><MessageSquare size={16}/>Message pour l’Extra</span><textarea value={message} onChange={event=>setMessage(event.target.value)} placeholder="Horaires, tenue, consignes ou informations utiles…" className="mt-2 min-h-28 w-full rounded-xl border bg-background p-3 font-normal"/></label></div>{error&&<p className="mt-4 rounded-xl bg-destructive/10 p-3 text-sm font-semibold text-destructive">{error}</p>}<div className="mt-5 flex flex-wrap gap-2"><button type="button" onClick={submit} disabled={!job||!location.trim()||busy} className="rounded-xl bg-primary px-4 py-2.5 font-bold text-primary-foreground disabled:opacity-40">{busy?'Envoi…':'Envoyer la demande'}</button><button type="button" onClick={onClose} className="rounded-xl border px-4 py-2.5 font-bold">Annuler</button></div></div></div>;
}
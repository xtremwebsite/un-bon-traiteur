import {CalendarDays,MapPin} from 'lucide-react';
import {formatAvailabilityDate} from '@/lib/extraAvailability';
const labels={pending:'En attente',confirmed:'Acceptée',declined:'Refusée'};
const colors={pending:'bg-chart-5 text-primary',confirmed:'bg-chart-2 text-primary-foreground',declined:'bg-muted text-muted-foreground'};
export default function ProBookingFeed({items}){
  const counts={pending:0,confirmed:0,declined:0};items.forEach(item=>counts[item.status]++);
  return <section className="mt-6 rounded-3xl border bg-card p-5 shadow-lg"><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-2"><CalendarDays className="text-primary"/><h2 className="text-xl font-bold">Suivi de mes réservations</h2></div><div className="flex flex-wrap gap-2 text-xs font-bold"><span className="rounded-full bg-chart-5 px-3 py-1 text-primary">{counts.pending} en attente</span><span className="rounded-full bg-chart-2 px-3 py-1 text-primary-foreground">{counts.confirmed} acceptée(s)</span><span className="rounded-full bg-muted px-3 py-1 text-muted-foreground">{counts.declined} refusée(s)</span></div></div>{items.length?<div className="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{items.map(item=><article key={item.id} className="rounded-2xl border bg-background p-4"><div className="flex items-start justify-between gap-2"><div><p className="font-bold">{item.extra_name||'Extra'}</p><p className="text-sm capitalize text-muted-foreground">{formatAvailabilityDate(item.booking_date)}</p></div><span className={`rounded-full px-2 py-1 text-xs font-bold ${colors[item.status]}`}>{labels[item.status]}</span></div><p className="mt-3 flex gap-2 text-sm"><MapPin className="shrink-0 text-primary" size={16}/>{item.location||'Lieu non renseigné'}</p><p className="mt-2 text-sm"><b>Prestation :</b> {item.service_details||'Non renseignée'}</p></article>)}</div>:<p className="mt-4 text-sm text-muted-foreground">Vous n’avez encore envoyé aucune demande de réservation.</p>}</section>;
}
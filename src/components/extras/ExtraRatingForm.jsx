import {useState} from 'react';
export default function ExtraRatingForm({extra,onSubmit,onClose}){const[data,setData]=useState({rating:5,comment:'',mission_date:''});const[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);await onSubmit({...data,extra_id:extra.id});setBusy(false);onClose()};return <form onSubmit={submit} className="mt-4 space-y-3 rounded-2xl bg-secondary p-4"><p className="font-bold">Recommander {extra.first_name}</p><div className="grid grid-cols-2 gap-3"><label>Note<select value={data.rating} onChange={e=>setData(x=>({...x,rating:e.target.value}))} className="mt-1 h-10 w-full rounded-xl border bg-background px-3">{[5,4,3,2,1].map(x=><option key={x} value={x}>{x} / 5</option>)}</select></label><label>Date de mission<input type="date" value={data.mission_date} onChange={e=>setData(x=>({...x,mission_date:e.target.value}))} className="mt-1 h-10 w-full rounded-xl border bg-background px-3"/></label></div><textarea required value={data.comment} onChange={e=>setData(x=>({...x,comment:e.target.value}))} className="min-h-20 w-full rounded-xl border bg-background p-3" placeholder="Votre recommandation professionnelle"/><div className="flex gap-2"><button disabled={busy} className="rounded-xl bg-primary px-4 py-2 font-bold text-primary-foreground">Enregistrer</button><button type="button" onClick={onClose} className="rounded-xl border px-4 py-2">Annuler</button></div></form>;}
import {useState} from 'react';
import {ChevronDown,Crosshair,Search,SlidersHorizontal} from 'lucide-react';
import ExtraLocationField from '@/components/extras/ExtraLocationField';

export default function ExtraOpportunityFilters({value,onChange,onSubmit,locating,onLocate,roles=[]}){
  const[open,setOpen]=useState(false);const set=(key,next)=>onChange({...value,[key]:next});
  return <form onSubmit={e=>{e.preventDefault();onSubmit();setOpen(false)}} className="rounded-2xl border bg-card/95 p-2 shadow-lg backdrop-blur-xl sm:p-3">
    <button type="button" onClick={()=>setOpen(current=>!current)} aria-expanded={open} className="flex w-full items-center justify-between gap-3 rounded-xl px-2 py-2 text-sm font-bold lg:hidden"><span className="flex items-center gap-2"><SlidersHorizontal size={17}/>Filtres de proximité</span><span className="flex items-center gap-2 text-xs text-muted-foreground">{value.radius_km} km<ChevronDown size={17} className={open?'rotate-180':''}/></span></button>
    <div className={`${open?'grid':'hidden'} mt-2 items-end gap-3 border-t pt-3 text-sm sm:grid-cols-2 lg:mt-0 lg:grid lg:grid-cols-[minmax(240px,1.5fr)_130px_minmax(180px,1fr)_170px_auto] lg:border-0 lg:pt-0`}><div className="relative"><ExtraLocationField cityOnly label="Ville" required={false} value={value.city} onChange={city=>onChange({...value,city,latitude:null,longitude:null})} onSelect={place=>onChange({...value,city:place.location,latitude:place.latitude,longitude:place.longitude})}/><button type="button" onClick={onLocate} disabled={locating} className="absolute right-2 top-7 rounded-lg bg-secondary p-2 text-primary" aria-label="Utiliser ma position"><Crosshair size={16}/></button></div><label className="block">Rayon<select value={value.radius_km} onChange={e=>set('radius_km',Number(e.target.value))} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"><option value="10">10 km</option><option value="25">25 km</option><option value="50">50 km</option><option value="75">75 km</option><option value="100">100 km</option></select></label><label className="block">Métier<select value={value.role} onChange={e=>set('role',e.target.value)} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"><option value="">Tous les métiers</option>{roles.map(role=><option key={role} value={role}>{role}</option>)}</select></label><label className="block">Jour<input type="date" value={value.date} onChange={e=>set('date',e.target.value)} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"/></label><button type="submit" className="flex h-11 items-center justify-center gap-2 rounded-xl bg-primary px-5 font-bold text-primary-foreground"><Search size={16}/>Rechercher</button></div>
  </form>;
}
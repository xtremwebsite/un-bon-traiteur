import {Crosshair,Search} from 'lucide-react';import ExtraLocationField from '@/components/extras/ExtraLocationField';
export default function ExtraOpportunityFilters({value,onChange,onSubmit,locating,onLocate,roles=[]}){const set=(key,next)=>onChange({...value,[key]:next});return <form onSubmit={e=>{e.preventDefault();onSubmit()}} className="mt-6 rounded-3xl border bg-card p-4 shadow-lg"><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-[1.4fr_.6fr_1fr_1fr]"><div className="relative"><ExtraLocationField label="Ville" required={false} value={value.city} onChange={city=>onChange({...value,city,latitude:null,longitude:null})} onSelect={place=>onChange({...value,city:place.location,latitude:place.latitude,longitude:place.longitude})}/><button type="button" onClick={onLocate} disabled={locating} className="absolute right-2 top-7 rounded-lg bg-secondary p-2 text-primary" aria-label="Utiliser ma géolocalisation"><Crosshair size={17}/></button></div><label>Rayon<select value={value.radius_km} onChange={e=>set('radius_km',Number(e.target.value))} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"><option value="10">10 km</option><option value="25">25 km</option><option value="50">50 km</option><option value="100">100 km</option></select></label><label>Type de mission<select value={value.role} onChange={e=>set('role',e.target.value)} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"><option value="">Tous les métiers</option>{roles.map(role=><option key={role} value={role}>{role}</option>)}</select></label><label>Jour<input type="date" value={value.date} onChange={e=>set('date',e.target.value)} className="mt-1 h-11 w-full rounded-xl border bg-background px-3"/></label></div><button type="submit" className="mt-3 flex items-center gap-2 rounded-xl bg-primary px-5 py-2.5 text-sm font-bold text-primary-foreground"><Search size={17}/>Rechercher</button></form>}
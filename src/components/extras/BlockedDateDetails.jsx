import {Building2,CalendarDays,Mail,MapPin,Phone,X} from 'lucide-react';import {formatAvailabilityDate} from '@/lib/extraAvailability';
const labels={pending:'En attente de réponse',confirmed:'Réservation confirmée'};
export default function BlockedDateDetails({booking,onClose}){if(!booking)return null;return <div className="fixed inset-0 z-[1100] grid place-items-center bg-foreground/50 p-4" role="dialog" aria-modal="true" aria-labelledby="blocked-date-title" onClick={onClose}><div className="w-full max-w-md rounded-3xl border bg-card p-5 shadow-2xl" onClick={e=>e.stopPropagation()}><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-widest text-destructive">Créneau bloqué</p><h3 id="blocked-date-title" className="mt-1 text-xl font-bold">{labels[booking.status]}</h3></div><button type="button" onClick={onClose} aria-label="Fermer" className="rounded-full p-2 hover:bg-secondary"><X size={20}/></button></div><div className="mt-5 space-y-3 text-sm"><p className="flex items-center gap-2"><CalendarDays size={17}/><b>{formatAvailabilityDate(booking.booking_date)}</b></p><p className="flex items-center gap-2"><Building2 size={17}/>{booking.caterer_name||'Traiteur non renseigné'}</p><p className="flex items-start gap-2"><MapPin size={17} className="mt-0.5 shrink-0"/>{booking.location||booking.caterer_address||booking.caterer_city||'Lieu non renseigné'}</p><p><b>Prestation :</b> {booking.service_details||'Non renseignée'}</p>{booking.status==='confirmed'&&<div className="space-y-2 rounded-2xl bg-secondary/50 p-3"><p><b>Contact confirmé</b></p><a href={`tel:${booking.caterer_phone||''}`} className="flex items-center gap-2"><Phone size={15}/>{booking.caterer_phone||'Téléphone non renseigné'}</a><a href={`mailto:${booking.caterer_email||''}`} className="flex items-center gap-2"><Mail size={15}/>{booking.caterer_email||'E-mail non renseigné'}</a></div>}</div><button type="button" onClick={onClose} className="mt-5 w-full rounded-xl bg-primary px-4 py-3 font-bold text-primary-foreground">Fermer</button></div></div>}
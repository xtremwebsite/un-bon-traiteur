import {Mail,Phone} from 'lucide-react';
export default function ExtraExpandedDetails({extra}){const transport=Array.isArray(extra.transport)?extra.transport:extra.transport?[extra.transport]:[];const labels={none:'Sans transport',public:'Transports en commun',vehicle:'Véhicule personnel',both:'Véhicule et transports'};return <div className="border-t bg-secondary/10 px-5 py-4 text-sm"><h3 className="font-bold">Fiche professionnelle</h3>{extra.experience_details&&<p className="mt-2">{extra.experience_details}</p>}<div className="mt-3 flex flex-wrap gap-2">{extra.clothing_size&&<span className="rounded-full bg-secondary px-2.5 py-1">Tenue {extra.clothing_size.toUpperCase()}</span>}{extra.height_cm&&<span className="rounded-full bg-secondary px-2.5 py-1">{(extra.height_cm/100).toLocaleString('fr-FR',{minimumFractionDigits:2})} m</span>}{(extra.languages||[]).map(language=><span key={language} className="rounded-full border px-2.5 py-1">{language}</span>)}{transport.map(item=><span key={item} className="rounded-full border px-2.5 py-1">{labels[item]||item}</span>)}</div><div className="mt-3 flex flex-wrap gap-3">{extra.email&&<a href={`mailto:${extra.email}`} className="flex items-center gap-1 font-semibold text-primary underline"><Mail size={15}/>{extra.email}</a>}{extra.phone&&<a href={`tel:${extra.phone}`} className="flex items-center gap-1 font-semibold text-primary underline"><Phone size={15}/>{extra.phone}</a>}</div>{extra.recommendations?.map((item,index)=><blockquote key={index} className="mt-3 border-l-2 border-primary pl-3 text-muted-foreground">“{item.comment}” — {item.caterer_name}, {item.rating}/5</blockquote>)}</div>;}
import {CalendarDays,Eye,MapPin,Users} from 'lucide-react';import {formatAvailabilityDate} from '@/lib/extraAvailability';
const labels={open:'Ouverte',filled:'Pourvue',cancelled:'Annulée'};
export default function ProRequestFeed({items,bookings}){const openItems=items.filter(item=>item.status==='open');return <section className="mb-6 rounded-3xl border bg-card p-5 shadow-lg"><h2 className="text-xl font-bold">Mes annonces publiées</h2><div className="mt-4 grid gap-3">{openItems.map(item=>{const candidates=bookings.filter(booking=>booking.extra_request_id===item.id);return <article key={item.id} className="rounded-2xl border bg-background p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-bold">{item.role}</p><p className="mt-1 flex items-center gap-2 text-sm text-muted-foreground"><CalendarDays size={15}/>{formatAvailabilityDate(item.event_date)}</p></div><span className="rounded-full bg-secondary px-3 py-1 text-xs font-bold">{labels[item.status]||item.status}</span></div><p className="mt-2 flex items-center gap-2 text-sm"><MapPin size={15}/>{item.location}</p><div className="mt-3 flex flex-wrap gap-4 text-xs font-semibold text-muted-foreground"><span className="flex items-center gap-1"><Eye size={14}/>{item.view_count||0} vue(s)</span><span className="flex items-center gap-1"><Users size={14}/>{candidates.length} candidature(s)</span></div></article>})}{!openItems.length&&<p className="text-sm text-muted-foreground">Aucune annonce publiée.</p>}</div></section>}
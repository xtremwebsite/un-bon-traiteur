import {CalendarDays,Eye,MapPin,Pencil,Trash2,Users} from 'lucide-react';
import {formatAvailabilityDate} from '@/lib/extraAvailability';

export default function PublishedRequestList({items=[],bookings=[],onEdit,onDelete}){
  const requests=items.filter(item=>item.status==='open');
  return <section className="rounded-3xl border bg-card p-5 shadow-lg"><h2 className="text-xl font-bold">Mes annonces publiées</h2><div className="mt-4 grid gap-3">{requests.map(item=>{const candidates=bookings.filter(x=>x.extra_request_id===item.id);return <article key={item.id} className="rounded-2xl border bg-background p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-bold">{item.required_count} × {item.role}</p><p className="mt-1 flex items-center gap-2 text-sm text-muted-foreground"><CalendarDays size={15}/>{formatAvailabilityDate(item.event_date)}</p></div><span className="rounded-full bg-secondary px-3 py-1 text-xs font-bold">Ouverte</span></div><p className="mt-2 flex items-center gap-2 text-sm"><MapPin size={15}/>{item.location} · {item.radius_km||15} km</p><div className="mt-3 flex flex-wrap items-center gap-3 text-xs font-semibold text-muted-foreground"><span className="flex items-center gap-1"><Eye size={14}/>{item.view_count||0} vue(s)</span><span className="flex items-center gap-1"><Users size={14}/>{candidates.length} candidature(s)</span><button type="button" onClick={()=>onEdit(item)} className="ml-auto flex items-center gap-1 text-primary"><Pencil size={14}/>Modifier</button><button type="button" onClick={()=>onDelete(item)} className="flex items-center gap-1 text-destructive"><Trash2 size={14}/>Supprimer</button></div></article>})}{!requests.length&&<p className="text-sm text-muted-foreground">Aucune annonce publiée.</p>}</div></section>;
}